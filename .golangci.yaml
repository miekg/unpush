version: "2"

run:
  concurrency: 4
  tests: true
  timeout: 5m

linters:
  default: none
  enable:
    - bodyclose
    - dupl
    - gochecknoinits
    - goconst
    - ineffassign
    - misspell
    - nakedret
    - unconvert
    - whitespace

formatters:
  enable:
    - gofumpt
    - goimports
